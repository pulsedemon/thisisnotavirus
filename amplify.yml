version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm install 20
        - nvm use 20
        - node -v
        - yarn install
    build:
      commands:
        - echo "API_BASE_URL=$API_BASE_URL" > .env
        - echo "VITE_SENTRY_DSN=$VITE_SENTRY_DSN" >> .env
        - yarn run build:prod

  artifacts:
    baseDirectory: dist
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
