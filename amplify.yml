version: 1
frontend:
  phases:
    preBuild:
      commands:
        - yarn install
        - yarn run build:prod
    # IMPORTANT - Please verify your build commands
    build:
      commands: []
  artifacts:
    # IMPORTANT - Please verify your build output directory
    baseDirectory: /
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
